<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Add New Blog</title>
  <link rel="stylesheet" href="/path/to/bootstrap.css"> <!-- Optional: Include Bootstrap for styling -->
  <style>
    body {
      background-color: #212121;
    }

    .form {
      display: flex;
      flex-direction: column;
      width: 500px;
      margin: 70px auto;
      gap: 15px;
      padding: 2em;
      background-color: #171717;
      border-radius: 25px;
      transition: .4s ease-in-out;
    }

    .form:hover {
      transform: scale(1.05);
      border: 1px solid black;
    }

    h1 {
      text-align: center;
      margin-bottom: 20px;
      color: white;
    }

    .field {
      display: flex;
      flex-direction: column;
      gap: 10px;
    }

    label {
      color: white;
      font-weight: bold;
    }

    .input-field, .file-input {
      border-radius: 25px;
      padding: 0.6em;
      border: none;
      outline: none;
      color: white;
      background-color: #252525;
      box-shadow: inset 2px 5px 10px rgb(5, 5, 5);
    }

    .input-field {
      width: 100%;
      color: #d3d3d3;
    }

    .btn {
      padding: 0.8em;
      border-radius: 5px;
      border: none;
      outline: none;
      transition: .4s ease-in-out;
      /* background-color: #252525; */
      color: black;
      cursor: pointer;
    }

    .btn:hover {
      color: red;
      
    }
    .form .btn {
      display: flex;
      justify-content: center;
      margin-top: 20px;
    }

    .top-links {
            display: flex;
            justify-content: space-between;
            margin: 20px;
        }

        .top-links a {
            color: #000;
            width: 20%;
            text-decoration: none;
            font-size: 18px;
            padding: 10px 20px;
            background-color: white;
            border-radius: 5px;
            transition: background-color 0.3s ease;
            text-align: center;
        }

        .top-links a:hover {
            background-color: #000;
            color: white;
        }
  </style>
</head>
<body>
  <div class="top-links">
    <a href="/dashboard">Back To Dashboard</a>
    <a href="/my-blogs">View My Blog</a>
    <a href="/blogs">View All Blog</a>
</div>
  <form class="form" action="/blogs/add" method="POST" enctype="multipart/form-data">
    <h1>Add a New Blog Post</h1>

    <!-- Title Input -->
    <div class="field">
      <label for="title">Blog Title</label>
      <input type="text" class="input-field" id="title" name="title" placeholder="Enter blog title" required>
    </div>

    <!-- Content Textarea -->
    <div class="field">
      <label for="content">Blog Content</label>
      <textarea class="input-field" id="content" name="content" rows="6" placeholder="Enter blog content" required></textarea>
    </div>

    <!-- Image Upload Input -->
    <div class="field">
      <label for="blogImage">Upload Image</label>
      <input type="file" class="file-input" id="blogImage" name="blogImage" accept="image/*" required>
    </div>

    <!-- Submit Button -->
    <div class="btn">
      <button type="submit" class="btn">Add Blog</button>
    </div>
  </form>

  <!-- Optional: Add Bootstrap JS or other JS files -->
  <script src="/path/to/bootstrap.js"></script>
</body>
</html>
